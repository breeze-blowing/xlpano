// 默认俯仰可视范围角度
export const DefaultYRange = [-86, 86];

// 默认静态可视范围角度
export const DefaultFovy = 60;

// 默认转场动画时间
export const DefaultSceneSwitchDuration = 500;

// 默认转场动画视角改变速度
export const DefaultSceneSwitchFovySpeed = 0.5;

// 移动灵敏度
export const DefaultMovingRate = 1.5;
